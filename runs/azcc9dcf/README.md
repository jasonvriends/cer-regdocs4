# azcc9dcf

The reference extraction, not ours. These are the cer-regdocs2 stage scripts that called Azure Content Understanding (`prebuilt-layout`, API `2025-11-01`), copied here because that repository is not a dependency of this one and the analysis cannot be re-run from here.
